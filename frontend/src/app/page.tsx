"use client"

import { ReactFlow, ReactFlowProvider } from "@xyflow/react";

export default function HomePage() {
  return (
   <>
   <ReactFlowProvider>
    <ReactFlow

    />
   </ReactFlowProvider>
   </>
  );
}
